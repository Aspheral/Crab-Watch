{
  "name": "crab-watch",
  "version": "0.11.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "vendor:stockfish": "npm install && node scripts/vendor-stockfish.mjs",
    "validate:release": "node scripts/validate-release.mjs",
    "package:extension": "npm run validate:release && node scripts/package-extension.mjs"
  },
  "dependencies": {
    "stockfish": "18.0.8"
  }
}
