{
  "name": "crab-watch",
  "version": "0.9.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "vendor:stockfish": "npm install && node scripts/vendor-stockfish.mjs"
  },
  "dependencies": {
    "stockfish": "18.0.8"
  }
}
